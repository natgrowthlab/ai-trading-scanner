[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-trading-scanner-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115,<1.0", "uvicorn[standard]>=0.30,<1.0", "pydantic>=2.8,<3.0",
  "pydantic-settings>=2.4,<3.0", "sqlalchemy>=2.0,<3.0", "psycopg[binary]>=3.2,<4.0",
  "alembic>=1.13,<2.0", "celery[redis]>=5.4,<6.0"
]
[project.optional-dependencies]
dev = ["pytest>=8.0", "httpx>=0.27", "ruff>=0.6"]
[tool.pytest.ini_options]
testpaths = ["tests", "../../packages/structure/tests"]
[tool.ruff]
line-length = 100
[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.setuptools.packages.find]
include = ["app*"]
