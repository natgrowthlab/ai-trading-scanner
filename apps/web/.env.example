# Public Binance Spot market data requires no key. Do not expose private API keys as NEXT_PUBLIC_* values.
# Optional server-side override for the market-data host. The built-in public fallbacks remain active.
BINANCE_PUBLIC_API_BASE_URL=
# BINANCE_API_KEY=
# BINANCE_API_SECRET=
