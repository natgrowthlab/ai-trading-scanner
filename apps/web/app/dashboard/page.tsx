"use client";

const modules = [["Live market data", "Binance USDT‑M perpetual ticker, depth and closed candles are read through the server."], ["Strategy", "Trend-pullback logic evaluates closed candles only, keeping each decision testable."], ["Execution", "Dry-run/Testnet only. No live order route or exchange key is present in the web app."]];

export default function DashboardPage() {
  return <main><p className="eyebrow">BINANCE FUTURES BOT</p><h1>Control center</h1><p className="lead">One product, one market focus: USDT‑M crypto futures. The production interface displays real market data without exposing exchange credentials.</p><section>{modules.map(([title, detail]) => <article key={title}><strong>{title}</strong><p>{detail}</p></article>)}</section><section className="status-grid" aria-label="Bot status"><article><p className="detail-label">Market feed</p><strong className="status-value ready">BINANCE FUTURES</strong><p>Public perpetual market data with regional endpoint fallback.</p></article><article><p className="detail-label">Bot mode</p><strong className="status-value warning">DRY RUN</strong><p>Signals and risk logic can be validated without creating exchange orders.</p></article><article><p className="detail-label">Execution</p><strong className="status-value pending">DISABLED</strong><p>Testnet is the next controlled step; live execution is intentionally unavailable.</p></article></section><p className="disclaimer">Crypto futures are high risk. Historical or simulated results never guarantee future outcomes.</p></main>;
}
