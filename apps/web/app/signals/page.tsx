export default function SignalsPage() {
  return <main><p className="eyebrow">SIGNALS</p><h1>Signal history</h1><p className="lead">The API exposes an empty history until the scanner generates validated signals. No simulated trades or outcomes are shown.</p><p className="disclaimer">Endpoint: /api/v1/signals</p></main>;
}
