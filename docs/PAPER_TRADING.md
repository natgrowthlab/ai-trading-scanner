# Paper trading

PaperAccount is an in-memory, immutable virtual ledger. It opens and closes simulated positions, records realized P&L, retains history, and rejects duplicate ids. It has no broker connection, credentials, order-routing code, or authority to trade real funds.
